# Compatibility

## Supported agent/runtime versions

| Component | Supported |
| --- | --- |
| Claude Code | Latest stable channel with slash-command skills support |
| Codex plugin runtime | Latest stable channel with skills/commands support |
| Bash | POSIX-compatible shell with `bash` available |

Compatibility target is maintained on `main`. Older tags may miss newer wrappers or playbooks.
